# Postgres (Supabase in production; local Postgres for dev)
DATABASE_URL=postgres://localhost:5432/filmfund_dev

# Claude API — extraction (Haiku) + classification/resolution (Opus)
ANTHROPIC_API_KEY=

# Firecrawl — permitted HTML sources only (ToS gate enforces)
FIRECRAWL_API_KEY=

# Email verification (Phase 4) — leave empty until enabled
EMAIL_VERIFY_API_KEY=
